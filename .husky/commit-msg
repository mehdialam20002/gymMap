#!/usr/bin/env sh
# A-24 · commit-msg — Conventional Commits carrying a PRD identifier.
# Grammar: PROJECT_CONSTITUTION.md §20.3

. "$(dirname "$0")/common.sh"
resolve_pnpm || exit 1

$PNPM exec commitlint --edit "$1"
