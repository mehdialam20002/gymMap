{
  "name": "@gymmap/ui",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "//": "Consumed as SOURCE, not as build output — Next.js transpilePackages and Vite both compile workspace TS directly. A dist/ step here would add a build edge to every front-end change for no benefit.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "build": "echo \"no-op\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no-op\"",
    "architecture": "echo \"no-op\"",
    "test:unit": "echo \"no-op\"",
    "test:int": "echo \"no-op\"",
    "test:contract": "echo \"no-op\"",
    "test:isolation": "echo \"no-op\"",
    "test:e2e": "echo \"no-op\"",
    "test:a11y": "echo \"no-op\"",
    "size": "echo \"no-op\"",
    "openapi:check": "echo \"no-op\""
  },
  "devDependencies": {
    "@gymmap/config": "workspace:*",
    "typescript": "^6.0.3"
  }
}
