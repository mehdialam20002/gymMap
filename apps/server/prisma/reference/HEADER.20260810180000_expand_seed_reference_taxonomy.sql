-- migration: 20260810180000_expand_seed_reference_taxonomy
-- phase:            expand                     -- MG3
-- requirement:      BLK-21 RESOLVED, ADR-0042, NFR-DQ-06, FR-GYM-03, FR-SRCH-03,
--                   SeedStrategy.md SEP1, RD2, RD3, RD4, RD5, §3.7
-- tables:           amenities (59 rows), gym_categories (15 rows) — INSERT only
-- rls:              NONE — GLOBAL reference on the closed §1.3 exemption list
-- grants:           unchanged. G-REF minus the write half; rows are written by the migration role
-- append_only:      no UPDATE and no DELETE are granted to any application role
-- partitioned:      no
-- max_lock:         ROW EXCLUSIVE on two tables holding zero rows
-- rewrite:          none
-- est_duration:     < 40 ms                                               -- MG11
-- backfill_job:     none. This IS the data.
-- rollback:         DELETE both tables' rows by their derived ids. gyms.category_id and
--                   gym_amenities.amenity_id reference them, and both tables are empty
-- concurrent_steps: none
-- reviewers:        two, one schema owner, one product   -- MG8
-- docs:             docs/DECISION_LOG.md ADR-0042 · SeedStrategy.md §3.7 · PHASES.md BLK-21
--
-- ═══════════════════════════════════════════════════════════════════════════════════════════════
-- GENERATED BY `pnpm ref:generate` FROM `prisma/reference/*.csv`. DO NOT EDIT BY HAND.
--
-- `RD4`: the data file is the source of truth and this file is its output. A hand edit here that
-- does not match the CSV fails `reference-data-drift` (§2.7), which hashes both.
--
-- ┌─ EVERY `key` IS §3.7 VERBATIM; ONLY THE DISPLAY LAYER IS NEW ────────────────────────────────┐
-- │ 59 amenity keys in six display groups (Facilities 16 · Equipment 16 · Classes 12 ·            │
-- │ Services 6 · Accessibility 6 · Hours 3) and 15 category keys, transcribed unchanged. What     │
-- │ `ADR-0042` adds is `name`, `icon`, `slug` and `sort_order` — the columns `Schema.md` §12.2     │
-- │ makes `NOT NULL` and no document fixes.                                                        │
-- │                                                                                              │
-- │ That asymmetry is the whole reason this was safe to decide: `RD2` derives every `id` from the │
-- │ KEY, so the ids are permanent and the display columns are editable through the `FR-ADMN-06`   │
-- │ path. `Epic_04.md` line 349 says so itself — *"retrofitting terms is cheap, retrofitting ids  │
-- │ is not"*.                                                                                       │
-- └──────────────────────────────────────────────────────────────────────────────────────────────┘
--
-- ┌─ NO `ON CONFLICT` — `RD5` ───────────────────────────────────────────────────────────────────┐
-- │ A plain INSERT, allowed to fail loudly. Under forward-only migrations a duplicate can only    │
-- │ mean this ran twice, which is itself the defect. Note this points the OPPOSITE way to `PM-9`'s │
-- │ `IF NOT EXISTS`: that rule is about DDL being re-appliable, this one is about DML being loud. │
-- └──────────────────────────────────────────────────────────────────────────────────────────────┘
-- ═══════════════════════════════════════════════════════════════════════════════════════════════

SET LOCAL lock_timeout       = '5s';    -- PM-8. first statement, always
SET LOCAL statement_timeout  = '300s';  -- PM-8. second statement, always

