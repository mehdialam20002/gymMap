-- migration: 20260810190000_expand_seed_reference_cities
-- phase:            expand                     -- MG3
-- requirement:      BLK-21 RESOLVED (geography half), ADR-0042, NFR-DQ-06, C9.4, TM3,
--                   SeedStrategy.md §3.6, SEP1, RD2, RD4, RD5, ADR-0007
-- tables:           cities (12 rows) — INSERT only
-- rls:              NONE — GLOBAL reference on the closed §1.3 exemption list
-- grants:           unchanged. G-REF minus the write half; rows written by the migration role
-- append_only:      no UPDATE and no DELETE are granted to any application role
-- partitioned:      no
-- max_lock:         ROW EXCLUSIVE on a table holding zero rows
-- rewrite:          none
-- est_duration:     < 30 ms                                               -- MG11
-- backfill_job:     none. This IS the data.
-- rollback:         DELETE the twelve rows by their derived ids. gyms.city_id and branches.city_id
--                   reference them; both tables are empty
-- concurrent_steps: none. gix_cities__centroid already exists and indexes twelve rows instantly
-- reviewers:        two, one schema owner      -- MG8
-- docs:             docs/DECISION_LOG.md ADR-0042 · SeedStrategy.md §3.6 · PHASES.md BLK-21
--
-- ═══════════════════════════════════════════════════════════════════════════════════════════════
-- GENERATED BY `pnpm ref:generate` FROM `prisma/reference/cities.csv`. DO NOT EDIT BY HAND.
--
-- ┌─ THE SLUGS ARE §3.6 VERBATIM; THE NAMES AND CENTROIDS ARE FACTS, NOT CHOICES ────────────────┐
-- │ §3.6 fixes twelve metro slugs, their GST state codes, `status = 'PLANNED'` and                │
-- │ `timezone = 'Asia/Kolkata'` for all twelve. It fixes no display `name` and no `centroid`,      │
-- │ and `Schema.md` §12.1 makes both `NOT NULL`. That gap is what `BLK-21` was about.               │
-- │                                                                                              │
-- │ It is a different KIND of gap from the amenity names, and that is why this half is safer      │
-- │ still. `mumbai` → *"Mumbai"* is not a product decision, and Mumbai's centroid is not a taste   │
-- │ question — it is a fact about the world. The four coordinates `Marketplace.md` §10.1 happens   │
-- │ to carry (Mumbai, Bengaluru, Pune, Hyderabad) match these to four decimal places, which is     │
-- │ corroboration rather than authority: that block is banner-marked `illustrative`.               │
-- └──────────────────────────────────────────────────────────────────────────────────────────────┘
--
-- ┌─ EVERY CITY IS `PLANNED`, AND NONE OF THEM IS THE LAUNCH CITY ───────────────────────────────┐
-- │ §3.6: *"`OQ-01` fixed the launch COUNTRY; the launch CITY is selected separately against the  │
-- │ `C9.4` criteria. Until then every Indian city is `PLANNED`."* Registered as open item `O-5`.   │
-- │ Nothing here promotes a city, and flipping one to `LIVE` is a `C9.4` gate decision with five   │
-- │ measurable criteria behind it — not a data edit.                                                │
-- └──────────────────────────────────────────────────────────────────────────────────────────────┘
--
-- ┌─ `ST_MakePoint` TAKES LONGITUDE FIRST ───────────────────────────────────────────────────────┐
-- │ The single easiest thing to get wrong in this file. Swapped, Mumbai lands in the Indian Ocean │
-- │ off Somalia — a valid point, on the right planet, that no "is this a geography" check would   │
-- │ catch. The CSV names its columns `latitude` and `longitude`, and the generator maps them, so   │
-- │ the swap has to be made deliberately rather than by ordering two anonymous numbers.             │
-- │                                                                                              │
-- │ `geography`, never `geometry` — ADR-0007. `gix_cities__centroid` is GiST and already exists.   │
-- └──────────────────────────────────────────────────────────────────────────────────────────────┘
--
-- No `ON CONFLICT`, per `RD5`. A duplicate under forward-only migrations means this ran twice.
-- ═══════════════════════════════════════════════════════════════════════════════════════════════

SET LOCAL lock_timeout       = '5s';    -- PM-8. first statement, always
SET LOCAL statement_timeout  = '300s';  -- PM-8. second statement, always

