{
  "name": "@gymmap/gym-dashboard",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "//": "M-001 · §3.6 script contract. build/dev become real in the milestone that scaffolds the framework; they are no-ops now rather than absent so `turbo run` never passes by omission.",
  "scripts": {
    "build": "echo \"no-op\"",
    "dev": "echo \"no-op\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings=0",
    "architecture": "echo \"no-op\"",
    "test:unit": "echo \"no-op\"",
    "test:int": "echo \"no-op\"",
    "test:contract": "echo \"no-op\"",
    "test:isolation": "echo \"no-op\"",
    "test:e2e": "echo \"no-op\"",
    "test:a11y": "echo \"no-op\"",
    "size": "echo \"no-op\"",
    "openapi:check": "echo \"no-op\""
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@gymmap/config": "workspace:*",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.66.0"
  },
  "//lint": "R1a keeps the ROOT devDependencies to seven packages and eslint is not one of them, so each workspace that lints declares its own runner (A-21). Turned on while this app is still one file, deliberately: a lint script that has never passed is easy to add and a lint script that has never run is what lets a thousand errors accumulate before anyone looks."
}
