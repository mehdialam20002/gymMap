node_modules/
dist/
build/
.next/
.turbo/
coverage/
playwright-report/
test-results/
pnpm-lock.yaml
*.tsbuildinfo

# Generated Prisma client
**/prisma/generated/

# Terraform state
*.tfstate
*.tfstate.*
.terraform/

# The specification corpus is hand-formatted and reviewed as prose. Prettier
# would reflow ~90,000 lines and destroy the table alignment. Format checks
# still apply to any NEW markdown outside docs/.
docs/

# Generated by packages/ui/src/tokens/generate-css.ts, and diffed against that generator by
# `pnpm --filter @gymmap/ui tokens:check`. Prettier reformatting it makes the two disagree, so
# the gate fails on a file nobody edited — two tools cannot both own one file's bytes.
packages/ui/src/tokens/tokens.css

# Next.js writes this during `next build`.
apps/*/next-env.d.ts
